# AOA-11 — Load Balancer Finish/Verification Report

Branch: `aoa/AOA-11`

## State found on entry

- `scripts/load_balancer.py` already implemented live per-pool quota collection, capability/load ordering through the existing router/PTME code, projected quota reservations, fail-closed refusal, decision/learning logs, QA pair rotation, and dispatch-request construction.
- `scripts/dispatch.py` already contained `dispatch_auto()` and an opt-in `--auto` CLI path.
- `aoa.config.json` already contained the load-balancer policy/pool configuration.
- `tests/test_load_balancer.py` already contained 32 passing tests, including selected/refused dispatch paths and both normal and owner-override attempts against the worker/QA separation gate.
- `dashboard/agent_activity.js` and `dashboard/agent_activity.json` contained one completed local Claude telemetry entry generated by `scripts/agent_telemetry.py`; they contained no load-balancer schema or presentation changes.

## Verification and fixes

- Read `scripts/load_balancer.py` and `dispatch_auto()` end-to-end.
- Confirmed `scripts/dispatch.py` is the documented common dispatch CLI used by real callers (`README.md`, `docs/dispatch.md`, and `scripts/parallel_dispatch.py` reference it).
- Closed the CLI reachability gap: omitting `--engine` now uses `dispatch_auto()` by default. Explicit `--engine` remains the direct-adapter/diagnostic path; `--auto` remains an explicit alias. Auto dispatch now also propagates `--enable-mcp`.
- Added `test_dispatch_cli_without_engine_uses_load_balancer`, proving the public CLI's no-engine path calls the balancer with task text and role.
- Confirmed the worker != QA gate is applied before candidate ordering and cannot be bypassed by quota override. Evidence: `test_worker_engine_excluded_from_qa_candidates` and `test_worker_qa_gate_is_unbypassable_even_with_owner_override`.
- Added `test_exhausted_preferred_pool_routes_to_eligible_idle_pool`: Codex is deliberately first in candidate order but has 0% remaining; the balancer excludes it and selects eligible Claude instead of refusing or blindly dispatching Codex.
- Updated `docs/dispatch.md` with the default load-balanced CLI form and the QA `--worker-engine` requirement.

## Dashboard decision

Both dashboard activity files were restored to their committed empty seed state. Their diff was generated runtime data (agent `claude-general-purpose-a3b6c33a`, completed/idle, timestamp `2026-07-17T11:47:40Z`) and did not expose routing decisions. Committing it would violate the repository rule that runtime dashboard history is not release data.

## Tests actually run

- Entry baseline: `python -m pytest` -> **351 passed**.
- Focused after fixes: `python -m pytest tests/test_load_balancer.py tests/test_dispatch.py -q` -> **47 passed**.
- Final full suite: `python -m pytest` -> **353 passed in 18.33s**.
- `git diff --check` passed.

No tests were skipped, weakened, or removed.

## Remaining independent QA

No known implementation work remains. QA should:

1. Run `python -m pytest` and require all 353 tests to pass.
2. Inspect the two invariant tests and the exhausted-pool test named above in `tests/test_load_balancer.py`.
3. Inspect `test_dispatch_cli_without_engine_uses_load_balancer` and `scripts/dispatch.py` to confirm no-engine dispatch is load-balanced while explicit `--engine` remains manual.
4. Run `git show --name-status HEAD` and confirm neither `dashboard/agent_activity.js` nor `dashboard/agent_activity.json` is in the commit.
5. Optional environment smoke check only: `python scripts/load_balancer.py quota` should show redacted local pool readings; unknown/stale pools must remain unknown rather than appearing as 100%.
