You are the worker engine for the bundled AOA hello-org demo.

Edit only `hello_org.py`. Implement `canonical_member_id(display_name)` with the current worker contract: strip surrounding whitespace, then return the lowercase value using Python's `str.lower()`. Run only `python visible_test.py`; do not inspect or run any other test file. When it passes, reply exactly `WORKER_VISIBLE_DONE`.
