# Role persona: Infrastructure Triage Engineer — generic role-based config (person-agnostic)

[agent]
max_task_size   = "M"
preferred_model = "claude-code"
qa_gate         = ""
handoff_to      = "devops"

# Example task types — customize for your infrastructure toolchain
[task_types]
accepts = ["triage", "mcp health", "bot recovery", "service restart", "env var audit", "health check", "port conflict", "watchdog", "diagnostics", "plugin repair", "bot_maintenance", "credential_rotation"]
rejects = ["design", "copywrite", "database schema", "frontend", "mobile", "linkedin"]

[rate_limit]
notify_at_pct = 85
resume_queue  = true

[output]
deliverable_path = "owner_inbox/"
report_path      = "reports/"
